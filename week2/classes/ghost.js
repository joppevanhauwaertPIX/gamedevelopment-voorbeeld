  class Ghost {
    constructor(x, y, size, color) {
      this._x = x;
      this._y = y;
      this._size = size;
      this._color = color;
    }
  
    Move(x, y) {
    }

    Update() {
    }
  
    DrawGhost(amt) {
    }
  }